/* Qutbi — vehicle-aware multi-passenger booking flow */
(function(){
  const $ = id => document.getElementById(id);
  const VEHICLES = {
    '4-seater Sedan Car':{capacity:4,seatMap:false}, '6-seater Innova':{capacity:6,seatMap:false},
    'RTA':{capacity:50,seatMap:false}, '7-seater Innova':{capacity:7,seatMap:false},
    '10-seater Tempo Traveller':{capacity:10,seatMap:false}, '15-seater':{capacity:15,seatMap:false},
    '20-seater':{capacity:20,seatMap:false}, '25-seater':{capacity:25,seatMap:false},
    '50-seater':{capacity:50,seatMap:true}
  };
  const state={step:1,deck:null,seats:[],passengerNames:{},availability:null,submitted:false,lastBooking:null};
  document.addEventListener('DOMContentLoaded',init);

  function init(){
    $('year').textContent=new Date().getFullYear(); $('travelDate').value=QT.todayISO(); $('travelDate').min=QT.todayISO();
    $('route').addEventListener('change',onRouteChange); $('vehicleType').addEventListener('change',clearSeatSelection);
    $('passengerCount').addEventListener('input',onPassengerCountChange);
    ['totalAmount','amountPaid'].forEach(id=>$(id).addEventListener('input',updateMoney));
    $('mobile').addEventListener('input',e=>{e.target.value=e.target.value.replace(/\D/g,'').slice(0,10);});
    $('btn-to-step-2').addEventListener('click',goToStep2); $('btn-back-1').addEventListener('click',()=>showStep(1));
    $('btn-to-step-3').addEventListener('click',goToStep3); $('btn-edit').addEventListener('click',()=>showStep(1));
    $('btn-confirm').addEventListener('click',confirmBooking); $('btn-print').addEventListener('click',()=>state.lastBooking&&QT.printTicket(state.lastBooking));
    $('btn-download').addEventListener('click',()=>state.lastBooking&&QT.downloadTicket(state.lastBooking));
    $('btn-new').addEventListener('click',()=>{resetForm();showStep(1);}); onRouteChange(); updateMoney();
  }
  function vehicleConfig(){return VEHICLES[$('vehicleType').value]||null;}
  function onRouteChange(){const indore=$('route').value==='INDORE_SURAT';$('f-nagar').style.display=indore?'':'none';if(!indore){$('boardingNagar').value='';$('f-nagar').classList.remove('invalid');}clearSeatSelection();}
  function routeLabel(r){return({NASHIK_SURAT:'Nashik to Surat',INDORE_SURAT:'Indore to Surat',SURAT_NASHIK:'Surat to Nashik',SURAT_INDORE:'Surat to Indore'})[r]||'';}
  function routePoints(r){if(r==='INDORE_SURAT')return{boarding:$('boardingNagar').value,dropping:'Surat'};if(r==='SURAT_NASHIK')return{boarding:'Surat',dropping:'Nashik'};if(r==='SURAT_INDORE')return{boarding:'Surat',dropping:'Indore'};return{boarding:'Nashik',dropping:'Surat'};}
  function updateMoney(){const total=Number($('totalAmount').value||0),paid=Number($('amountPaid').value||0);$('remainingAmount').value=QT.fmtMoney(Math.max(0,total-paid));$('paymentStatus').value=QT.paymentStatusFor(total,paid);$('f-paid').classList.toggle('invalid',paid>total&&total>0);}
  function validateStep1(){let ok=true;const set=(id,v)=>{$(id).classList.toggle('invalid',!v);if(!v)ok=false;};const r=$('route').value;set('f-route',['NASHIK_SURAT','INDORE_SURAT','SURAT_NASHIK','SURAT_INDORE'].includes(r));if(r==='INDORE_SURAT')set('f-nagar',['Ammar Nagar','Nurai Nagar','Sefi Nagar'].includes($('boardingNagar').value));set('f-mobile',QT.validMobile($('mobile').value));set('f-vehicle',!!vehicleConfig());set('f-date',QT.isFutureOrToday($('travelDate').value));const total=Number($('totalAmount').value),paid=Number($('amountPaid').value);set('f-total',total>=0&&$('totalAmount').value!=='');set('f-paid',paid>=0&&paid<=total&&$('amountPaid').value!=='');set('f-method',$('paymentMethod').value!=='');return ok;}

  async function goToStep2(){
    if(!validateStep1())return QT.toast('Please fix the highlighted fields.','err'); clearSeatSelection();
    const cfg=vehicleConfig(),vehicle=$('vehicleType').value,trip=routeLabel($('route').value)+' on '+QT.fmtDate($('travelDate').value);
    $('seatDateLabel').textContent=trip; $('seatSectionTitle').textContent=cfg.seatMap?'Select seats and passengers':'Passenger details';
    $('seatSectionSubtitle').textContent=cfg.seatMap?'Select available seats for '+trip+', then enter the passenger for each seat.':'Choose how many people are travelling in the '+vehicle+', then enter every passenger name.';
    showStep(2); toggleBookingMode(cfg);
    if(cfg.seatMap){state.deck='Seater';await loadAvailability();renderSeatMap();}
    else{$('passengerCount').max=String(cfg.capacity);$('passengerCapacity').textContent=String(cfg.capacity);$('passengerCount').value='1';setPassengerCount(1);}
  }
  function toggleBookingMode(cfg){$('seatMapControls').style.display=cfg.seatMap?'':'none';$('seatLegend').style.display=cfg.seatMap?'':'none';$('busFrame').style.display=cfg.seatMap?'':'none';$('passengerCountCard').style.display=cfg.seatMap?'none':'';$('ss-seat-row').style.display=cfg.seatMap?'':'none';$('passengerNamesHint').textContent=cfg.seatMap?'Enter the passenger travelling on each selected seat.':'Enter one full name for every passenger.';}
  async function loadAvailability(){QT.overlay(true,'Checking seat availability…');try{const res=await QT.api('seatStatus',{route:$('route').value,travelDate:$('travelDate').value,vehicleType:$('vehicleType').value});if(!res.ok)throw new Error(QT.friendlyError(res.error));state.availability=res;const booked=res.booked||{};state.seats=state.seats.filter(s=>!booked[s.id]);renderSelectedSeats();}catch(err){QT.toast(err.message||'Failed to load seats','err');state.availability={booked:{}};}finally{QT.overlay(false);}}
  function generate50Seats(){const seats=[];for(let i=1;i<=50;i++){const col=i<=40?[1,2,4,5][(i-1)%4]:((i-41)%5)+1;seats.push({id:'S'+i,num:i,deck:'Seater',col});}return seats;}
  function renderSeatMap(){const grid=$('busGrid');grid.className='bus-grid seater-50-grid';grid.innerHTML='';const booked=(state.availability&&state.availability.booked)||{};generate50Seats().forEach(s=>{const el=document.createElement('button');el.type='button';el.className='seat seater-seat';el.style.gridColumn=String(s.col);const b=booked[s.id],selected=state.seats.some(x=>x.id===s.id);if(b){el.classList.add('booked');el.disabled=true;el.innerHTML=`<div class="no">${s.num}</div><div class="who">${escapeHTML(b.passengerName||'Booked')}</div>`;}else{el.classList.add(selected?'selected':'avail');el.innerHTML=`<div class="no">${s.num}</div><div class="who">${selected?'Selected':'Available'}</div>`;el.addEventListener('click',()=>toggleSeat(s));}grid.appendChild(el);});}
  function toggleSeat(s){const idx=state.seats.findIndex(x=>x.id===s.id);if(idx>=0){state.seats.splice(idx,1);delete state.passengerNames[s.id];}else state.seats.push({id:s.id,deck:s.deck});state.seats.sort((a,b)=>Number(a.id.slice(1))-Number(b.id.slice(1)));renderSeatMap();renderSelectedSeats();}
  function onPassengerCountChange(){const cfg=vehicleConfig();if(!cfg||cfg.seatMap)return;let count=Math.floor(Number($('passengerCount').value||0));count=Math.max(1,Math.min(cfg.capacity,count));$('passengerCount').value=String(count);setPassengerCount(count);}
  function setPassengerCount(count){const old=Object.assign({},state.passengerNames);state.seats=Array.from({length:count},(_,i)=>({id:'P'+(i+1),deck:'Passenger'}));state.passengerNames={};state.seats.forEach(s=>{state.passengerNames[s.id]=old[s.id]||'';});renderSelectedSeats();}
  function displaySeat(s){if(s.id.charAt(0)==='P')return'Passenger '+s.id.slice(1);if(s.id.charAt(0)==='S')return'Seat '+s.id.slice(1);return s.id;}
  function renderSelectedSeats(){const has=state.seats.length>0;$('seatSummary').style.display=has?'':'none';$('passengerNamesCard').style.display=has?'':'none';$('btn-to-step-3').disabled=!has;if(!has){$('passengerNameFields').innerHTML='';return;}$('ss-seat').textContent=state.seats.map(displaySeat).join(', ');$('ss-count').textContent=String(state.seats.length);$('ss-date').textContent=QT.fmtDate($('travelDate').value);const wrap=$('passengerNameFields');wrap.innerHTML=state.seats.map(s=>`<div class="passenger-seat-field" data-seat="${s.id}"><span class="seat-tag">${escapeHTML(displaySeat(s))}</span><input type="text" data-passenger-seat="${s.id}" placeholder="Passenger full name" value="${escapeAttr(state.passengerNames[s.id]||'')}"></div>`).join('');wrap.querySelectorAll('input[data-passenger-seat]').forEach(input=>input.addEventListener('input',e=>{state.passengerNames[e.target.dataset.passengerSeat]=e.target.value;e.target.parentElement.classList.remove('invalid');}));}
  function validatePassengerNames(){let ok=true;state.seats.forEach(s=>{const name=String(state.passengerNames[s.id]||'').trim(),field=document.querySelector(`.passenger-seat-field[data-seat="${s.id}"]`),valid=QT.validName(name);if(field)field.classList.toggle('invalid',!valid);if(!valid)ok=false;});return ok;}
  function collectBooking(){const total=Number($('totalAmount').value||0),paid=Number($('amountPaid').value||0),route=$('route').value,points=routePoints(route);const passengers=state.seats.map(s=>({passengerName:String(state.passengerNames[s.id]||'').trim(),seatNumber:s.id,sleeperType:s.deck,seatLabel:displaySeat(s)}));return{route,routeLabel:routeLabel(route),mobile:$('mobile').value.trim(),vehicleType:$('vehicleType').value,travelDate:$('travelDate').value,boardingPoint:points.boarding,droppingPoint:points.dropping,totalAmount:total,amountPaid:paid,remainingAmount:Math.max(0,total-paid),paymentMethod:$('paymentMethod').value,paymentStatus:QT.paymentStatusFor(total,paid),notes:$('notes').value.trim(),passengers,bookedBy:'Staff'};}
  function passengerHTML(b){return b.passengers.map(p=>`${escapeHTML(p.seatLabel||p.seatNumber)} — ${escapeHTML(p.passengerName)}`).join('<br>');}
  function goToStep3(){if(!state.seats.length)return QT.toast(vehicleConfig().seatMap?'Select at least one seat.':'Enter the passenger count.','err');if(!validatePassengerNames())return QT.toast('Enter a name for every passenger.','err');const b=collectBooking();const rows=[['Route',escapeHTML(b.routeLabel)],['Boarding point',escapeHTML(b.boardingPoint)],['Dropping point',escapeHTML(b.droppingPoint)],['Mobile number',escapeHTML(b.mobile)],['Vehicle / transport',escapeHTML(b.vehicleType)],['Travel date',QT.fmtDate(b.travelDate)],['Passengers / seats',passengerHTML(b)],['Total ticket amount',QT.fmtMoney(b.totalAmount)],['Amount paid',QT.fmtMoney(b.amountPaid)],['Remaining amount',QT.fmtMoney(b.remainingAmount)],['Payment method',escapeHTML(b.paymentMethod)],['Payment status',QT.statusPill(b.paymentStatus)]];$('reviewPanel').innerHTML=rows.map(([k,v])=>`<div class="row"><span class="k">${k}</span><span class="v">${v}</span></div>`).join('');showStep(3);}
  async function confirmBooking(){if(state.submitted)return;if(!validatePassengerNames())return QT.toast('Enter every passenger name.','err');state.submitted=true;const btn=$('btn-confirm');btn.disabled=true;btn.innerHTML='<span class="spinner"></span> Saving…';QT.overlay(true,'Confirming booking…');const b=collectBooking();try{if(vehicleConfig().seatMap){const avail=await QT.api('seatStatus',{route:b.route,travelDate:b.travelDate,vehicleType:b.vehicleType});if(!avail.ok)throw new Error(avail.error||'Failed to check seats');const taken=b.passengers.filter(p=>avail.booked&&avail.booked[p.seatNumber]).map(p=>p.seatNumber);if(taken.length)throw new Error('SEAT_TAKEN');}const res=await QT.api('createBooking',{booking:b});if(!res.ok)throw new Error(res.error||'Failed to save');b.bookingId=res.groupBookingId;b.bookingIds=res.bookingIds||[];b.passengerName=b.passengers.map(p=>p.passengerName).join(', ');b.seatNumber=b.passengers.map(p=>p.seatLabel||p.seatNumber).join(', ');b.sleeperType='Multiple';b.paymentStatus=res.paymentStatus;b.remainingAmount=res.remaining;state.lastBooking=b;renderSuccess(b);showStep(4);QT.toast('Every passenger was saved to Google Sheets','ok');}catch(err){QT.toast(QT.friendlyError(err.message),'err');state.submitted=false;btn.disabled=false;btn.textContent='Confirm booking ✓';if(err.message==='SEAT_TAKEN'){await loadAvailability();renderSeatMap();showStep(2);}}finally{QT.overlay(false);}}
  function renderSuccess(b){const rows=[['Group booking ID',`<b>${escapeHTML(b.bookingId)}</b>`],['Route',escapeHTML(b.routeLabel)],['Boarding',escapeHTML(b.boardingPoint)],['Vehicle / transport',escapeHTML(b.vehicleType)],['Passengers / seats',passengerHTML(b)],['Travel date',QT.fmtDate(b.travelDate)],['Total ticket amount',QT.fmtMoney(b.totalAmount)],['Amount paid',QT.fmtMoney(b.amountPaid)],['Remaining amount',QT.fmtMoney(b.remainingAmount)],['Payment status',QT.statusPill(b.paymentStatus)]];$('confirmPanel').innerHTML='<div class="summary">'+rows.map(([k,v])=>`<div class="row"><span class="k">${k}</span><span class="v">${v}</span></div>`).join('')+'</div><div style="margin-top:16px">'+QT.ticketHTML(b)+'</div>';$('btn-whatsapp').href=QT.whatsappLink(b.mobile,QT.bookingWhatsappMessage(b));}
  function showStep(n){state.step=n;for(let i=1;i<=4;i++){const el=$('section-'+i);if(el)el.style.display=i===n?'':'none';}document.querySelectorAll('.stepper .step').forEach(s=>{const num=Number(s.dataset.step);s.classList.toggle('active',num===n);s.classList.toggle('done',num<n);});window.scrollTo({top:0,behavior:'smooth'});}
  function clearSeatSelection(){state.seats=[];state.passengerNames={};state.availability=null;state.deck=null;if($('passengerNameFields'))renderSelectedSeats();}
  function resetForm(){['mobile','totalAmount','amountPaid','notes'].forEach(id=>$(id).value='');$('route').value='';$('boardingNagar').value='';$('paymentMethod').value='';$('vehicleType').value='';$('travelDate').value=QT.todayISO();$('passengerCount').value='1';state.submitted=false;state.lastBooking=null;clearSeatSelection();$('btn-confirm').textContent='Confirm booking ✓';document.querySelectorAll('.field.invalid').forEach(f=>f.classList.remove('invalid'));onRouteChange();updateMoney();}
  function escapeHTML(s){return String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
  function escapeAttr(s){return escapeHTML(s).replace(/`/g,'&#96;');}
})();
